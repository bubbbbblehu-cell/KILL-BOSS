# 📁 项目结构详细说明

## 🎯 整体架构

```
BOSS KILL
├── 前端 (Frontend)
│   ├── HTML/CSS/JS (原生，无框架)
│   └── ES6 Modules 模块化架构
│
├── 后端 (Backend)
│   └── Supabase (BaaS)
│       ├── PostgreSQL 数据库
│       ├── Auth 认证
│       └── Storage 存储
│
└── 部署 (Deployment)
    └── Vercel (静态部署)
```

## 📂 目录结构详解

### 根目录文件

| 文件 | 说明 |
|------|------|
| `index.html` | 主 HTML 文件，包含所有页面结构 |
| `style.css` | 全局样式文件 |
| `main.js` | ⚠️ 旧入口文件（已废弃，保留作为备份） |
| `package.json` | Node.js 项目配置（用于后端服务器） |
| `.env` | 环境变量配置（Supabase URL 和 Key） |
| `database_setup.sql` | 数据库创建脚本（可直接执行） |

### 文档目录

| 文档 | 用途 |
|------|------|
| `README.md` | 项目主文档，快速开始指南 |
| `DATABASE_SCHEMA.md` | 数据库表结构详细设计 |
| `FEATURE_ROADMAP.md` | 功能开发路线图 |
| `MODULE_STRUCTURE.md` | 模块结构详细说明 |
| `SQL_SETUP_GUIDE.md` | SQL 数据库设置指南 |
| `SUPABASE_CHECKLIST.md` | Supabase 连接问题排查 |
| `LOGIN_TEST_GUIDE.md` | 登录功能测试指南 |
| `MIGRATION_GUIDE.md` | 代码模块化迁移说明 |
| `PROJECT_STRUCTURE.md` | 本文件，项目结构说明 |

### src/ 目录

```
src/
├── js/                    # JavaScript 源代码
│   │
│   ├── 核心模块 (Core Modules)
│   │   ├── main.js        # 主入口，导入所有模块
│   │   ├── app.js         # 应用初始化
│   │   ├── config.js      # 配置常量
│   │   ├── state.js       # 状态管理
│   │   ├── supabase.js    # Supabase 客户端
│   │   ├── auth.js        # 认证功能
│   │   ├── navigation.js  # 页面导航
│   │   ├── utils.js       # 工具函数
│   │   └── diagnostics.js # 诊断工具
│   │
│   └── modules/           # 功能模块
│       │
│       ├── swipe/         # 划一划发帖模块（泡鼠）
│       │   ├── swipeFeed.js    # 滑动Feed
│       │   ├── comments.js     # 评论
│       │   └── leaderboard.js  # 榜单
│       │
│       ├── post/          # 发帖模块（泡鼠）
│       │   ├── createPost.js   # 创建帖子
│       │   └── textEditor.js   # 文字编辑
│       │
│       ├── map/           # 地图模块（拆鼠）
│       │   └── mapView.js      # 地图视图
│       │
│       └── profile/        # 我的模块（拆鼠）
│           ├── myPosts.js      # 我的帖子
│           ├── favorites.js    # 我的收藏
│           └── notifications.js # 消息通知
│
└── server.js              # 后端服务器（可选）
```

## 🔄 模块依赖关系

```
main.js (入口)
  │
  ├─→ config.js (配置)
  │
  ├─→ state.js (状态)
  │   └─→ config.js
  │
  ├─→ supabase.js (Supabase客户端)
  │   └─→ config.js
  │
  ├─→ auth.js (认证)
  │   ├─→ supabase.js
  │   ├─→ state.js
  │   ├─→ utils.js
  │   └─→ navigation.js
  │
  ├─→ navigation.js (导航)
  │
  ├─→ utils.js (工具)
  │
  ├─→ diagnostics.js (诊断)
  │   └─→ config.js
  │
  ├─→ app.js (应用初始化)
  │   ├─→ supabase.js
  │   ├─→ state.js
  │   ├─→ navigation.js
  │   └─→ config.js
  │
  └─→ modules/ (功能模块)
      │
      ├─→ swipe/
      │   ├─→ swipeFeed.js → supabase.js, state.js
      │   ├─→ comments.js → supabase.js, state.js
      │   └─→ leaderboard.js → supabase.js
      │
      ├─→ post/
      │   ├─→ createPost.js → supabase.js, state.js, navigation.js
      │   └─→ textEditor.js
      │
      ├─→ map/
      │   └─→ mapView.js → supabase.js, state.js
      │
      └─→ profile/
          ├─→ myPosts.js → supabase.js, state.js
          ├─→ favorites.js → supabase.js, state.js
          └─→ notifications.js → supabase.js, state.js
```

## 📊 数据流

### 用户认证流程

```
用户操作
  ↓
auth.js (处理登录/注册)
  ↓
supabase.js (Supabase Auth API)
  ↓
state.js (更新应用状态)
  ↓
UI 更新
```

### 数据获取流程

```
功能模块 (如 swipeFeed.js)
  ↓
supabase.js (获取客户端)
  ↓
Supabase Database API
  ↓
返回数据
  ↓
更新 UI
```

## 🗂️ 文件职责说明

### 核心模块

#### main.js
- **职责**：主入口文件
- **功能**：导入所有模块，确保模块加载顺序
- **依赖**：所有其他模块

#### config.js
- **职责**：配置管理
- **功能**：存储 Supabase 配置、应用常量
- **依赖**：无

#### state.js
- **职责**：状态管理
- **功能**：管理全局应用状态（用户信息、数据等）
- **依赖**：config.js

#### supabase.js
- **职责**：Supabase 客户端管理
- **功能**：初始化客户端、会话管理、连接检查
- **依赖**：config.js

#### auth.js
- **职责**：用户认证
- **功能**：登录、注册、登出、游客模式
- **依赖**：supabase.js, state.js, utils.js, navigation.js

#### navigation.js
- **职责**：页面导航
- **功能**：页面切换、演示控制
- **依赖**：无

#### app.js
- **职责**：应用初始化
- **功能**：启动流程、会话恢复、数据库连接测试
- **依赖**：supabase.js, state.js, navigation.js, config.js

### 功能模块

#### swipe/ 划一划发帖模块

- **swipeFeed.js**：滑动浏览帖子，左右滑动交互
- **comments.js**：评论功能，显示和添加评论
- **leaderboard.js**：榜单功能，今日/周/月/年榜

#### post/ 发帖模块

- **createPost.js**：创建帖子，支持文字和图片
- **textEditor.js**：文字编辑器，字数统计

#### map/ 地图模块

- **mapView.js**：地图显示，便便和建筑标记

#### profile/ 我的模块

- **myPosts.js**：我的帖子列表和管理
- **favorites.js**：我的收藏列表和管理
- **notifications.js**：消息通知列表和管理

## 🔌 外部依赖

### CDN 资源

- **Supabase JS**：`https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2.39.0`
- **Google Fonts**：Noto Sans SC 字体

### 后端服务

- **Supabase**：
  - Database (PostgreSQL)
  - Auth (用户认证)
  - Storage (文件存储)

## 📝 代码组织原则

1. **模块化**：每个功能独立成模块
2. **单一职责**：每个文件只负责一个功能
3. **依赖注入**：通过 import/export 管理依赖
4. **全局访问**：需要 HTML 调用的函数挂载到 `window`
5. **配置集中**：所有配置集中在 `config.js`

## 🚀 扩展指南

### 添加新功能模块

1. 在 `src/js/modules/` 下创建新目录
2. 创建模块文件，导出函数
3. 在 `src/js/main.js` 中导入
4. 如需 HTML 调用，挂载到 `window`

### 添加新页面

1. 在 `index.html` 中添加页面结构
2. 在 `navigation.js` 中添加切换逻辑
3. 创建对应的模块文件处理页面逻辑

### 添加新数据库表

1. 在 `database_setup.sql` 中添加表结构
2. 在 `DATABASE_SCHEMA.md` 中更新文档
3. 在对应模块中添加数据操作代码
