-- ============================================
-- 创建测试用户脚本
-- 在 Supabase SQL Editor 中执行此脚本
-- ============================================

-- 方法1：通过 Supabase Auth API 创建用户（推荐）
-- 注意：Supabase 不允许直接通过 SQL 插入到 auth.users 表
-- 需要通过应用注册或 Supabase Dashboard 创建

-- ============================================
-- 方法2：在 Supabase Dashboard 中创建用户
-- ============================================
-- 1. 进入 Supabase Dashboard
-- 2. 点击左侧菜单 "Authentication"
-- 3. 点击 "Users" 标签
-- 4. 点击 "Add User" 或 "Invite User"
-- 5. 填写邮箱和密码
-- 6. 创建用户

-- ============================================
-- 方法3：在应用中注册用户（最简单）
-- ============================================
-- 1. 打开应用
-- 2. 点击"注册新账号"
-- 3. 输入邮箱和密码
-- 4. 完成注册
-- 5. 然后执行 insert_test_data.sql

-- ============================================
-- 验证用户是否存在
-- ============================================
SELECT id, email, created_at 
FROM auth.users 
ORDER BY created_at DESC;

-- 如果返回空结果，说明没有用户，需要先创建用户
